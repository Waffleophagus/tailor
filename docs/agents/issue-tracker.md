# Issue tracker

## Type

Local markdown

## Workflow

Issues are created as files under `.scratch/<feature>/` when needed. The project is currently taking a "less is more" approach — large roadmaps are intentionally pared back.
